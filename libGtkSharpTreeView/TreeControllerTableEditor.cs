using System;

namespace GtkSharp.Extensions
{
	/*
	 * A controller which allows the user to edit the model, 
	 * without the programmer having to mess around with the mapping functions
	 * 
	 * Perhaps it could take, for each column, two mapping functions
	 * 	 Cell Data Setter (cell.Value = object.Value)
	 *   Object Value Setter (object.Value = cell.Value)
	 */
	public class TreeControllerTableEditor
	{
		public TreeControllerTableEditor ()
		{
		}


	}
}

